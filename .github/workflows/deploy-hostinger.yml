name: Deploy to Hostinger VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: newproject-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Validate deployment secrets
        env:
          DEPLOY_WEBHOOK_SECRET: ${{ secrets.DEPLOY_WEBHOOK_SECRET }}
          VPS_DEPLOY_URL: ${{ secrets.VPS_DEPLOY_URL }}
        run: |
          missing=()
          [ -z "$DEPLOY_WEBHOOK_SECRET" ] && missing+=("DEPLOY_WEBHOOK_SECRET")
          [ -z "$VPS_DEPLOY_URL" ] && missing+=("VPS_DEPLOY_URL")

          if [ "${#missing[@]}" -gt 0 ]; then
            echo "::error::Missing GitHub Actions secrets: ${missing[*]}"
            echo "On VPS: add DEPLOY_WEBHOOK_SECRET to .env and set GitHub secrets:"
            echo "  DEPLOY_WEBHOOK_SECRET = same value as VPS .env"
            echo "  VPS_DEPLOY_URL = http://31.97.50.25:8081/api/deploy"
            exit 1
          fi

      - name: Trigger VPS deploy webhook
        env:
          DEPLOY_WEBHOOK_SECRET: ${{ secrets.DEPLOY_WEBHOOK_SECRET }}
          VPS_DEPLOY_URL: ${{ secrets.VPS_DEPLOY_URL }}
        run: |
          set -euo pipefail
          echo "Deploying commit ${GITHUB_SHA} via webhook"
          response=$(curl -fsS --max-time 600 -X POST "$VPS_DEPLOY_URL" \
            -H "Authorization: Bearer ${DEPLOY_WEBHOOK_SECRET}" \
            -H "Content-Type: application/json" \
            -d "{\"ref\":\"${GITHUB_REF}\",\"sha\":\"${GITHUB_SHA}\"}")
          echo "$response"
          echo "Deploy finished successfully."
